import path from 'path';

// Paths
export const PATH = {
    root: path.join(__dirname, '..'),
    tmp: path.join(__dirname, '..', 'tmp'),
    srv: path.join(__dirname, '..', 'srv'),
};

// Teeworld Github
export const GITHUB = {
    owner: 'teeworlds',
    repo: 'teeworlds',
};
